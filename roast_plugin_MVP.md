# roast_plugin --- MVP

## 目标

开发一个 Windows **全自动吐槽器**。

程序后台观察用户当前正在使用的应用和页面，在用户**切换界面**或**长时间停留**时，根据当前行为自动生成一句吐槽，并显示在桌面悬浮窗中。

核心流程：

`检测 PC 行为 → 判断触发 → LLM 生成吐槽 → 悬浮窗显示`

roast_plugin 只是吐槽器，不是 AI 助手、效率工具或桌面宠物。

------------------------------------------------------------------------

## 核心功能

### 1. 界面切换吐槽

监测：

-   当前应用
-   当前窗口标题
-   上一个窗口
-   停留时间
-   最近的窗口切换记录

应用或窗口内容发生明显变化时触发吐槽。

切换后等待约 **1--2
秒**，如果期间继续快速切换，则合并事件，避免连续刷屏。

例如：

`VSCode → Chrome`

> 写不出来就开始求助互联网了是吧。

`VSCode → Steam`

> 项目推进得不错，都推进到 Steam 了。

### 2. 长时间停留吐槽

用户持续停留在同一界面时也可以触发吐槽。

建议时间：

`5min → 15min → 30min → 60min`

每个时间节点只触发一次。

### 3. 短期行为上下文

保留最近约 **30 分钟**的窗口活动记录，让 LLM 能理解连续行为。

例如：

`VSCode → Chrome → VSCode → ChatGPT → VSCode`

可以生成：

> 你今晚在 VSCode 和互联网之间往返得挺勤快。

MVP 不需要长期记忆。

------------------------------------------------------------------------

## LLM 吐槽

向模型提供简洁的结构化上下文：

``` json
{
  "event": "WINDOW_SWITCH",
  "from": "VSCode",
  "to": "Steam",
  "title": "Limbus Company",
  "previous_duration": 2400,
  "recent_activity": []
}
```

生成要求：

-   每次只生成一句
-   默认中文
-   尽量 30 字以内
-   简短、自然、好笑、略带阴阳怪气
-   尽量结合当前具体行为
-   不解释
-   不提供建议
-   不进行效率提醒
-   避免重复近期吐槽

模型定位是 **commentator，而不是 assistant**。

------------------------------------------------------------------------

## UI

使用 **PySide6**。

只制作一个简洁的桌面悬浮吐槽窗：

-   无系统标题栏
-   始终置顶
-   用户可以自由拖动
-   重启后记住上次位置
-   不抢夺当前窗口焦点
-   吐槽出现几秒后自动隐藏/收起

示例：

``` text
┌─────────────────────────────┐
│ 写不出来就开始求助互联网了？ │
└─────────────────────────────┘
```

右键只需要：

`暂停吐槽 / 吐槽频率 / API 设置 / 退出`

不要制作 Dashboard、桌面角色、宠物系统、动画、统计页面或其他复杂 UI。

------------------------------------------------------------------------

## 技术

推荐：

-   Python
-   PySide6
-   pywin32
-   psutil
-   httpx
-   JSON / SQLite

MVP 不使用截图、OCR、键盘记录或屏幕录制。

------------------------------------------------------------------------

## 开发顺序

1.  获取当前窗口、进程和标题
2.  检测窗口切换
3.  检测停留时间
4.  保存最近活动记录
5.  实现可拖动悬浮窗
6.  使用固定文本测试完整触发流程
7.  接入 LLM API
8.  添加右键基础设置

优先完成可运行的完整闭环，不要提前扩展功能或过度设计架构。

## MVP 完成标准

**用户切换界面或长时间停留 → roast_plugin 自动理解当前行为 →
生成一句相关吐槽 → 悬浮窗显示。**
